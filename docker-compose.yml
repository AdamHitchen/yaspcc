version: '3.6'
services:
  nginx:
    build: nginx 
    links:
      - php
    ports: 
      - 443:443
      - 80:80
    volumes:
    - ./yaspcc:/yaspcc
  php:
    build: php
    environment:
      XDEBUG_CONFIG: remote_host=192.168.0.12
    links: 
      - redis
    volumes:
      - ./yaspcc:/yaspcc
  redis:
    image: redis
